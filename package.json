{
  "name": "endpass-homepage",
  "version": "1.0.0",
  "description": "Endpass home page",
  "author": "Dutch <dutch@unviral.com>",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "aws s3 sync --acl public-read dist/ s3://${S3_BUCKET_NAME}"
  },
  "dependencies": {
    "nuxt": "^1.0.0",
    "nuxt-sass-resources-loader": "^1.1.1"
  },
  "devDependencies": {
    "extract-text-webpack-plugin": "^3.0.2",
    "node-sass": "^4.7.2",
    "sass-loader": "^6.0.6"
  }
}
